# data 目錄

這個目錄用來放研究資料，但實際資料檔不提交到 git。

建議分層：

- `raw/`：原始匯出資料或 API 原始下載結果
- `cache/`：adapter 產生的 parquet 快取
- `processed/`：已清理、可直接研究使用的資料

注意事項：

- 不要提交 API token。
- 不要提交付費或授權受限資料。
- 台股若使用未還原價格，回測報表要標註限制。
- 台灣基金 NAV 之後可先放 CSV，再由 adapter 轉成標準格式。
